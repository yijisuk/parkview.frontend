import { StyleSheet } from "react-native";
import { commonStyles } from "../commonStyles";

const searchViewStyles = StyleSheet.create({
    container: {
        ...commonStyles.container,
        justifyContent: "center",
        alignItems: "center",
    },
});

export default searchViewStyles;
